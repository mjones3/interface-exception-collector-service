apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "interface-exception-collector.fullname" . }}-test-connection"
  labels:
    {{- include "interface-exception-collector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.35
      command: ['wget']
      args: ['{{ include "interface-exception-collector.fullname" . }}:{{ .Values.service.port }}/actuator/health']
      resources:
        limits:
          cpu: 100m
          memory: 128Mi
        requests:
          cpu: 50m
          memory: 64Mi