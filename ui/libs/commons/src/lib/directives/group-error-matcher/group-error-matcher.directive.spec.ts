import { GroupErrorMatcherDirective } from './group-error-matcher.directive';

describe('GroupErrorMatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new GroupErrorMatcherDirective();
    expect(directive).toBeTruthy();
  });
});
