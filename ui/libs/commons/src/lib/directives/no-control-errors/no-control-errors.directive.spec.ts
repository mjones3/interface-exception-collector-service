import { NoControlErrorsDirective } from './no-control-errors.directive';

describe('NoControlErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new NoControlErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
