apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "distribution-ui.fullname" . }}-test-connection"
  labels:
    {{- include "distribution-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "distribution-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
