# Flyway
FROM flyway/flyway:10.21.0-alpine AS flyway

############################################
### Install Flyway CLI
############################################
WORKDIR /flyway

COPY ./src/main/db ./db

ENV PATH="/flyway:${PATH}"
