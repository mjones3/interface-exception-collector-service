CREATE TABLE inventory.bld_inventory_property
(
    key                         VARCHAR(255) NOT NULL,
    value                       VARCHAR(255) NOT NULL,
    CONSTRAINT pk_bld_inventory_property PRIMARY KEY (key)
);
