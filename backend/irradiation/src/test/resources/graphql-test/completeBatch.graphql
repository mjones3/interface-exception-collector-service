mutation completeBatch($batchId: String!, $endTime: String!, $batchItems: [BatchItemCompletionInput!]!) {
    completeBatch(input: {
        batchId: $batchId,
        endTime: $endTime,
        batchItems: $batchItems
    }) {
        batchId
        message
        success
    }
}