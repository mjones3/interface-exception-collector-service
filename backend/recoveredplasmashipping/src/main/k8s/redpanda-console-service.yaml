apiVersion: v1
kind: Service
metadata:
  name: redpanda-console-service
  namespace: queue
spec:
  selector:
    app: redpanda-console
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8080
