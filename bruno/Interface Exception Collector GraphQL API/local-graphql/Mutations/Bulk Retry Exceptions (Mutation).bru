meta {
  name: Bulk Retry Exceptions (Mutation)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/graphql/mutation/bulk-retry-exceptions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "query": "",
    "variables": {}
  }
}
