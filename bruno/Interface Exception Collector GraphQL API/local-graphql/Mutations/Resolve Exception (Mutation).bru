meta {
  name: Resolve Exception (Mutation)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/graphql/mutation/resolve-exception
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "query": "",
    "variables": {}
  }
}
