meta {
  name: search exceptions
  type: http
  seq: 4
}

get {
  url: http://localhost:8080/api/v1/exceptions/search?query=suspended
  body: json
  auth: bearer
}

params:query {
  query: suspended
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
