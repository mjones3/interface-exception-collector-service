# Python 3.13 compatible dependencies for Kafka event generation scripts
# Using confluent-kafka as it has better Python 3.13 support

# Primary Kafka client (better Python 3.13 compatibility)
confluent-kafka>=2.3.0,<3.0.0

# HTTP client for REST API fallback
requests>=2.31.0,<3.0.0

# Date/time utilities
python-dateutil>=2.8.2,<3.0.0

# Colored terminal output
colorama>=0.4.6,<1.0.0

# JSON handling (usually built-in, but explicit for clarity)
# ujson>=5.8.0  # Uncomment for faster JSON processing if needed